services:
  app1:
    image: alpine
    command: sleep infinity
    networks:
      - private_net
  
  app2:
    image: alpine
    command: sleep infinity
    networks:
      - private_net

networks:
  private_net:
    driver: bridge
